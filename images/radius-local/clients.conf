client downstream {
    ipaddr = $ENV{RADIUS_CLIENT_ADDRESS}
    secret = $ENV{RADIUS_CLIENT_SECRET}
    require_message_authenticator = $ENV{RADIUS_CLIENT_REQUIRE_MESSAGE_AUTHENTICATOR}
    limit_proxy_state = $ENV{RADIUS_CLIENT_LIMIT_PROXY_STATE}
}
