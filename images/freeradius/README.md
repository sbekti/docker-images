# FreeRADIUS

FreeRADIUS 3.2.8 on Ubuntu 26.04, published for `linux/amd64` and
`linux/arm64` as `ghcr.io/sbekti/freeradius:v3.2.8`.

The image uses Ubuntu's distribution packages without a duplicate version pin.
Runtime version and architecture are verified as part of release acceptance.
